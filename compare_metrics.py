def compare_metrics():
    # МЕТРИКИ ДО улучшений
    metrics_before = {
        'P@5': 0.660, 'P@10': 0.540, 'nDCG@5': 0.680,
        'nDCG@10': 0.746, 'AvgP': 0.693, 'MRR': 0.750
    }

    # МЕТРИКИ ПОСЛЕ улучшений (замени на свои реальные результаты)
    metrics_after = {
        'P@5': 0.800,  # ЗАМЕНИ на свои результаты
        'P@10': 0.670,  # ЗАМЕНИ на свои результаты
        'nDCG@5': 0.849,  # ЗАМЕНИ на свои результаты
        'nDCG@10': 0.923,  # ЗАМЕНИ на свои результаты
        'AvgP': 0.857,  # ЗАМЕНИ на свои результаты
        'MRR': 1.000  # ЗАМЕНИ на свои результаты
    }

    print("📊 СРАВНЕНИЕ МЕТРИК ДО И ПОСЛЕ УЛУЧШЕНИЙ")
    print("=" * 65)
    print(f"{'Метрика':<15} {'До':<8} {'После':<8} {'Изменение':<12} {'Рост':<10}")
    print("-" * 65)

    for metric in metrics_before:
        before = metrics_before[metric]
        after = metrics_after[metric]
        change = after - before
        growth_pct = (change / before) * 100 if before > 0 else 0

        print(f"{metric:<15} {before:<8.3f} {after:<8.3f} {change:+.3f} ({growth_pct:+.1f}%)")


if __name__ == "__main__":
    compare_metrics()